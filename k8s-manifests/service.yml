apiVersion: v1
kind: Service
metadata:
  name: dev-cicd-challenge-service
spec:
  type: NodePort
  selector:
    app_name: dev-cicd-challenge-pod
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000